<?php 

?>

<?php  $options_data = "";
   $options_data .= sapp_Global::selectOptionBuilder('', 'Select Allocate To ');
if(!empty($this->employeeData))
{
	foreach ($this->employeeData as $empdata) {
		$options_data .= sapp_Global::selectOptionBuilder($empdata['user_id'],utf8_encode($empdata['userfullname']));
	}
	echo $options_data;
}else
{
  echo "noemployees";
}
?>